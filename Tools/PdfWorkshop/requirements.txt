# PDF Tool — Python dependencies
# Install all with:  pip install -r requirements.txt

# Core PDF reading/writing (required)
pypdf>=4.0.0

# Text extraction (required for Convert tab)
pdfplumber>=0.10.0

# Word document export (required for .docx conversion)
python-docx>=1.1.0

# Image export — PDF pages → PNG/JPG
# Also requires Poppler (see README.md for install instructions)
pdf2image>=1.17.0

# Optional: nice progress bars in CLI usage
tqdm>=4.60.0
